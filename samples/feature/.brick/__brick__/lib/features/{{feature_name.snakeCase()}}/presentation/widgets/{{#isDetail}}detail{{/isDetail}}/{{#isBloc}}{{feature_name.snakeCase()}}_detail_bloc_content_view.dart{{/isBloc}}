import 'package:flutter/material.dart';

class {{feature_name.pascalCase()}}DetailBlocContentView extends StatelessWidget {
  const {{feature_name.pascalCase()}}DetailBlocContentView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      key: const Key('{{feature_name.snakeCase()}}_detail_bloc_content_view'),
    );
  }
}
